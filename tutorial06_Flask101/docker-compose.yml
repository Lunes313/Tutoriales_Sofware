version: '3.8'

services:
  pagos_flask:
    build: ./microservicio_pagos
    ports:
      - "5000:5000"

  nginx:
    image: nginx:1.25-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - pagos_flask